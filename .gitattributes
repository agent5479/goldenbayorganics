# Normalize line endings across platforms
* text=auto eol=lf

# Windows scripts keep CRLF
*.{cmd,bat,ps1} text eol=crlf

# Binary assets
*.{jpg,jpeg,png,gif,webp,ico,svg,pdf,woff,woff2} binary

# Lockfile
package-lock.json merge=binary
